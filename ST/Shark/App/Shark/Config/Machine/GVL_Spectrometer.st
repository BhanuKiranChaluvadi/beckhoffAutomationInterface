(* CONFIG_TIER: 1-MACHINE  -- Update all values for every new machine build. *)
{attribute 'qualified_only'}
VAR_GLOBAL CONSTANT
    // Pixel count of the installed spectrometer detector array
    SPECTROMETER_PIXEL_COUNT : INT := 2048;
END_VAR
